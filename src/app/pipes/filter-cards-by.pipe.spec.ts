import { FilterCardsByPipe } from './filter-cards-by.pipe';

describe('FilterCardsByPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCardsByPipe();
    expect(pipe).toBeTruthy();
  });
});
